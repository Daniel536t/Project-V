Place product PNGs here (sourced locally for the demo, never hotlinked):

  iphone-17-pro.png   (primary product of record)
  hero-phone.png      (hero cluster center)
  airpods-pro.png     (hero cluster left / product card)
  macbook-air.png
  watch-s9.png
  airtag.png

If an image is missing, the UI renders a centered Package icon placeholder so the layout never breaks.
